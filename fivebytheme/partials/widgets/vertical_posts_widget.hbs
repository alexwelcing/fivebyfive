{{#get "posts" include="tags" filter="tags:[travel]" limit="3" as |widgetpostarticle|}}
    {{#if widgetpostarticle}}



<div class="mnmd-widget widget widget-posts-list">
    <div class="widget__inner widget-posts-list__inner">
        <div class="widget__title">
            <h4 class="widget__title-text">{{t "POPULAR"}}</h4>
            <p class="sub-heading__title">{{t "Sed do eiusmod tempor"}}</p>
        </div>
        <div class="widget__wrap widget-posts-list__wrap clearfix">
            <ul class="posts-list list-space-md list-unstyled clearfix">
                {{#foreach widgetpostarticle}}
                <li class="widget-posts-list__item widget-big-posts-list__item">
                    <article class="post post--vertical height-sm post-santoni">
                        {{#if feature_image}}
                        <div class="post__thumb">
                            <div class="background-img" style="background-image: url({{img_url feature_image size="s"}});"></div>
                            <a class="link-overlay" href="{{url}}"></a>
                            <div class="post-date">
                                <time class="time published" datetime="{{date published_at}}" title="{{date published_at}}">
                                    <p class="post-day">{{date published_at format="DD"}}</p>
                                    <span class="post-year"> {{date published_at format="MMMM"}}</span>
                                </time>
                            </div>
                        </div>
                        {{/if}}
                        <div class="post__text">
                            {{#primary_tag}}
                            <a class="post__cat" href="{{url}}">{{name}}</a>
                            {{/primary_tag}}
                            <h3 class="post__title"><a href="{{url}}">{{title}}</a></h3>
                        </div>
                    </article>
                </li>
                {{/foreach}}
            </ul>
        </div>
    </div>
</div>
    {{/if}}
{{/get}}